/* 슬라이드 7 · 가설 검증 — 4개 세우고 3개 기각 ★핵심 3장
   계획서 §6 '가설 검증 4건' 그대로. 기각=레드 / 채택=앰버.
   이 장의 메시지는 결과가 아니라 태도다 — 그럴듯한 설명을 계속 무너뜨렸다. */
'use strict';
module.exports = (pptx, T) => {
  const { C, G } = T;
  const { d } = T.shell(pptx, { sec: '가설 검증', idx: 3, step: 3 });

  T.title(d, '그럴듯한 설명 4개를 의심했고,', '*3개는 틀렸습니다*');
  T.lead(d, '온도에 따라 갈리는 이유를 설명할 후보를 세우고 데이터로 하나씩 검증했습니다. _살아남은 것은 하나_ 입니다.');

  const H = [
    { v: '기각', q: '2025 상반기는\n설비 수준이\n달랐다',
      e: '해당 5건의 IGV 효과 평균이 +0.09 였습니다. 설비가 아니라 _IGV 미실시_ 가 원인이었습니다.' },
    { v: '기각', q: '진공 잔차로\n보정값을\n예측할 수 있다',
      e: '5건일 때 관련성 −0.85 로 강했지만, _36건에서 +0.09_ 로 사라졌습니다. 표본 5개의 과대해석이었습니다.' },
    { v: '기각', q: '담당자 표와의\n이론값 차이는\n진공 항 때문이다',
      e: '58건을 대조하니 차이가 온도에 비례했습니다. 진공이 아니라 _곡선 자체의 차이_ 였습니다.' },
    { v: '채택', q: '진공도는\n외기온도의\n함수다',
      e: '진공도 = 33.64 + 1.242 × 온도, 관련성 *+0.947*. 온도 하나로 설명됩니다.' },
  ];
  H.forEach((h, i) => {
    const x = 72 + i * 289, ok = h.v === '채택';
    d.panel(x, 288, 269, ok ? 'on' : 'bad');
    d.plab('가설 ' + (i + 1), x, 302, 269, ok ? C.brass : C.red);
    d.sub(h.q, x, 324, 269, 3);
    d.box(x, 424, 74, 30, null, ok ? '6B5220' : C.redD, 1.4);
    d.text(h.v, { x, y: 431, w: 74, px: 14, lh: 1.2, bold: true,
                  color: ok ? C.brass : C.red, align: 'center' });
    d.txt(h.e, x, 472, 269, 4);
  });

  d.zone(G.L, 578, G.W, 42);
  d.text('표본 5개로 보였던 관련성 −0.85 는 36개에서 +0.09 로 사라졌습니다  ·  적은 데이터로 결론을 내리지 않는 것이 이 과제의 규칙입니다',
         { x: 96, y: 590, w: 1088, px: 13, lh: 1.4, color: C.dim, align: 'center' });

  d.hline(G.L, G.RULE2, G.W, C.rule, 1);
  T.foot(d, '남은 설명은 하나였습니다 — *온도*. 그래서 온도로 나눠 학습합니다.');
};
