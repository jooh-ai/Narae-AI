/* v2-07 · 데이터 분석 및 원인 규명 (2/2) 가설 검증 — 요소 2개: 가설 5행 / 재발 방지 띠.
   앞 장(6장)이 '어떻게 원인을 좁혔나' 라면 이 장은 '무엇을 의심하고 무엇을
   기각했나' 다. 판정 기준을 리드에 먼저 밝힌다 — 기준 없이 기각·채택만 보이면
   자의적으로 보인다.
   숫자 출처: 계획서 §6, docs/ppt/VACUUM_EFFECT.md, vacuum_effect_check.py.   */
'use strict';
const H = [
  ['기각', '2025 상반기는 설비 상태가 달랐다',
   '설비가 아니라 _IGV(공기 흡입량을 늘리는 조작) 미실시_ 였습니다. 효과 평균 *+0.09 MW*'],
  ['기각', '진공도를 보면 보정값을 맞힐 수 있다',
   '5회에서 r *−0.85* 로 보였지만, 40회 전부에서는 *+0.00* — 임계 0.31 미달'],
  ['기각', '이론값과의 차이는 진공 항 때문이다',
   '58회를 맞대 보니 차이가 *온도에 비례* — 진공이 아니라 _이론 곡선 자체의 차이_'],
  ['기각', '여름에만 진공도 영향이 보인다',
   '여름 9회 r *−0.675* (임계 0.666) → 창을 넓힐수록 0 으로 수렴. _창 9개 중 고른 값_'],
  ['채택', '진공도는 외기온도의 함수다',
   '진공도 = 33.43 + 1.261 × 온도,  r *+0.952* (설명력 91%) — 위 4건이 강해 보인 이유'],
];
module.exports = (pptx, T, meta, D) => {
  const { C, G } = T;
  const { d } = T.shell(pptx, { sec: '가설 검증', idx: 3, step: 3 });
  T.title(d, '그럴듯한 설명 5개를 세웠고,', '*4개는 기각했습니다*');
  T.lead(d, '기각 기준은 둘입니다 — _상관계수 r 이 임계값(±0.31)을 넘지 못하거나_, ' +
         '한 회만 빼도 결론이 뒤집히면 채택하지 않습니다.');

  H.forEach(([v, q, e], i) => {
    const y = 284 + i * 56, ok = v === '채택';
    d.box(G.L, y + 2, 64, 28, null, ok ? '6B5220' : C.redD, 1.4);
    d.text(v, { x: G.L, y: y + 8, w: 64, px: 13.5, lh: 1.2, bold: true,
                color: ok ? C.brass : C.red, align: 'center' });
    d.sub(q, 152, y, 430, 1, ok ? C.brass : C.ink);
    d.text(e, { x: 600, y: y + 6, w: 608, px: 13.5, lh: 1.68, lines: 2 });
    if (i < 4) d.hline(G.L, y + 53, G.W, C.rule2, 1);
  });

  /* 이 장의 결론 — 세 번 걸린 것을 절차로 막았다 */
  d.zone(G.L, 566, G.W, 58);
  d.text('같은 변수(진공도)에 *세 번* 걸렸습니다 — 전부 _부분집합을 잘라 만든 상관_ 이었습니다.  ' +
         '그래서 이 판정을 사람이 하지 않고 *도구 안의 절차로* 옮겼습니다 (11장 가드).',
         { x: 96, y: 576, w: 1088, px: 14.5, lh: 1.5, lines: 2, color: C.body,
           align: 'center' });

  d.hline(G.L, G.RULE2, G.W, C.rule, 1);
  T.foot(d, '남은 설명은 *온도* 하나 — 그래서 온도별 곡선을 학습하기로 했습니다.');
};
