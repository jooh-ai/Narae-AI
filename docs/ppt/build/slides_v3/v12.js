/* v3-13 · 마무리 — 감사 인사와 질의응답.

   2026-09-18 회신: "마지막 페이지에서 곡선은 지우고, 감사합니다와 Q&A
   적절히 조합해서 만들어줘."

   곡선을 걷었다. 그 그림(외기온도별 신고 출력)은 7장 「출력곡선 비교」 화면에
   도구 화면으로 이미 들어가 있어서 마지막 장에 한 번 더 두면 같은 말을 두 번
   하는 셈이었다. 마지막 장은 마지막 장 일만 한다.

   짜임새는 **표지와 짝이 되게** 잡았다. 같은 기준선(좌 72 · 헤어라인 150 ·
   꼬리글 662)과 같은 머리 모양(왼쪽 소속 · 오른쪽 작은 글씨)을 쓰므로 첫 장과
   끝 장이 한 쌍으로 읽힌다.

   2026-09-18 회신 "회사 CI 둘다 지우고, 질문 주시면~도 빼자" 를 반영해
   CI 와 안내 한 줄을 걷었다. 남은 것은 인사와 Q&A 뿐이므로 두 줄을 헤어라인
   사이(150 ~ 634) 가운데에 앉혔다 — 위아래 여백이 같아야 비운 것이 실수처럼
   보이지 않는다.

   흰 자리를 굳이 채우지 않았다. 앞 열두 장이 빽빽하므로 마지막 장의 여백이
   "끝났다" 는 신호가 된다.                                               */
'use strict';
module.exports = (pptx, T, meta, D) => {
  const { C, G } = T;
  /* 우상단 소제목은 끈다 — 왼쪽 소속 줄과 '위례사업소' 를 두 번 말하게 된다. */
  const { d } = T.shell(pptx, { topRight: false });
  d.text(meta.org + '   ·   ' + meta.when,
         { x: G.L, y: G.SEC_Y, w: 700, px: 12, lh: 1.25, mono: true,
           color: C.dim2, cs: 1.8 });

  d.hline(G.L, 150, G.W, C.rule, 1);

  /* 인사와 Q&A — 헤어라인 사이(150 ~ 634) 가운데.
     빨간 짧은 바 3 + 사이 28 + 감사합니다 70 + 사이 24 + Q&A 37 = 162 이므로
     위 여백 (484 − 162) / 2 ≒ 161 → 바를 311 에 둔다.
     Q&A 자간은 2pt. 8pt 로 두었더니 'Q  &  A' 로 흩어져 보였다.          */
  d.rect(G.L, 311, 46, 3, C.brass);
  d.text('감사합니다', { x: G.L, y: 339, w: 900, px: 58, lh: 1.2, bold: true,
                         color: C.ink });
  d.text('Q & A', { x: G.L, y: 433, w: 400, px: 31, lh: 1.2, mono: true, bold: true,
                    color: C.brass, cs: 2 });

  d.hline(G.L, 634, G.W, C.rule, 1);
  d.text(meta.authors.join('   ·   '), { x: G.L, y: G.FOOT_Y, w: 600, px: 14, lh: 1.4,
                                          color: C.dim });
  d.text('위례열병합발전소  ·  공급가능용량 입찰 산정 Tool',
         { x: 708, y: G.FOOT_Y + 2, w: 500, px: 12, lh: 1.4, mono: true, color: C.dim2,
           cs: 1.4, align: 'right' });
};
