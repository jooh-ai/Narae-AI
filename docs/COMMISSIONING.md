# 시운전 절차 — 새 테스트 실적으로 예측 정확도 검증

누적은 2026-04-02(31건)까지 반영돼 있고, 담당자로부터 2026-07까지 6건을 받은 상태를
전제로 한다. 이 6건은 **모델이 한 번도 본 적 없는 데이터**다. 그래서 지금이 예측
정확도를 제대로 측정할 수 있는 거의 유일한 기회다.

관련 문서: 첫 설치·RiMS 결선은 `ONSITE_TEST.md`, 배포·트러블슈팅은 `DEPLOY.md`.

---

## 0. 절대 규칙 — 예측을 먼저, 반영을 나중에

6건을 먼저 DB 에 넣고 나서 맞춰보면 **모델이 이미 그 값을 본 상태**다. 자기 데이터로
자기를 채점하는 셈이라 항상 좋게 나오고, 그 숫자는 실입찰에서 재현되지 않는다.

그래서 회차마다 이 순서를 지킨다. 순서를 어기면 그 회차는 폐기하고 다시 해야 한다.

```
예측 기록  →  실측 대조  →  반영  →  다음 회차
```

`scripts/commission.py` 가 이 순서를 강제한다. 예측은 항상 "반영 전 누적"으로 계산하고,
반영하는 순간의 예측값을 `commission_log.csv` 에 함께 적는다. 그 파일이 시운전 결과
보고서가 된다.

---

## 1. 섞으면 안 되는 세 가지

| 검증 | 무엇을 보는가 | 판정 | 언제 |
|---|---|---|---|
| ① 취득 일치 | RiMS 취득값이 담당자 표와 같은가 | 소수 4자리 | 반영 전 |
| ② 계산 일치 | 같은 입력에 같은 이론기준값·보정값이 나오는가 | ±0.25 MW | 반영 전 |
| ③ 예측 정확도 | 미리 낸 신고값이 실측과 맞는가 | ±0.5%(약 ±2 MW) | **반영 전만** |

①②는 결정론적 검사다 — 틀리면 버그이고, 값이 언제 확인되든 결과가 같다.
③만 순서에 민감하다. ①②가 통과하지 않은 건은 ③을 볼 의미가 없으니 먼저 잡는다.

> 참고: 실제 입찰은 **전날 예보 온도**로 낸다. 이 절차는 **실측 CIT**로 평가하므로
> 보정모델의 정확도만 본다. 예보 오차는 별개 항목이고, 섞으면 어느 쪽이 원인인지
> 구분할 수 없다.

---

## 2. 준비

**① 현재 상태 백업** — 되돌릴 수 있어야 마음 편히 진행한다.

```powershell
cd dist\WiryeBidTool          # 또는 tool 폴더(소스 실행 시)
copy wirye_measurements.db wirye_measurements_31건_백업.db
python -m wirye_capacity list | Select-Object -Last 5
```

기준선 확인: **31건, 마지막 2026-04-02**. 다르면 여기서 멈추고 원인을 먼저 확인한다.

**② 입력 서식 만들기**

```powershell
python scripts\commission.py --init
```

`commission_input.csv` 가 생긴다. 엑셀로 열어 담당자 표에서 6건을 옮겨 적는다.

| 열 | 내용 |
|---|---|
| `date` | 테스트 날짜 `YYYY-MM-DD` |
| `cit` | Comp. Inlet Temp (°C) |
| `press` | 대기압 (mbar) — Comp. Inlet Pres. 가 아니다 |
| `rh` | 상대습도 (%) |
| `cc_gross` | **CC(Gross)** 실측 (MW) — IGV 반영값 |
| `vac` | 진공도 (mbar), 없으면 비움 |
| `igv` | IGV Turn-up 실시 여부 `O`(기본) / `X`. **`X` 는 검증에서 아예 빠진다** |
| `w` | W(IGV) 직접 지정. 비우면 온도밴드 기본값(+6/+4/0) |
| `ref_theory` | 담당자 표의 이론기준값 → ② 검증에 쓴다 |
| `ref_corr` | 담당자 표의 보정값 → ② 검증에 쓴다 |

`ref_theory`·`ref_corr` 를 꼭 넣는다. 이게 없으면 ② 계산 일치를 건너뛰고, 나중에
차이가 났을 때 취득·계산·모델 중 무엇이 원인인지 못 가린다.

### ⚠ IGV Turn-up 미실시 시험은 쓰지 않는다 (담당자 방침)

**IGV 실시 여부에 따라 출력 변동이 너무 커서, 일관성을 위해 IGV Turn-up 을 실시한
시험만 보정값에 사용한다.** 미실시 시험은 W 를 어떻게 두든 누적 대상이 아니다.

왜 반드시 걸러야 하는가. 보정값 = `CC실측 − 이론기준값 − W(IGV)` 이므로, 실시하지
않은 turn-up 을 빼면 보정값이 **4~6 MW 낮게** 기록된다. 2026-08 시운전에서 8건 중
3건이 미실시였고, 그 3건이 전부 미달로 나와 모델이 틀린 것처럼 보였다. 더 나아가
2025 상반기 5건의 −4.09 MW 낙차를 "설비 열화" 로 오판하게 만든 원인도 같은 것이었다.
넣으면 그 회차만이 아니라 **보정곡선 전체가 오염된다.**

- 입력 CSV 의 `igv` 열에 **`X`** 를 적는다 → 검증·반영에서 자동으로 빠진다.
- 툴로 직접 취득할 때는 산정 탭의 **`IGV Turn-up 실시`** 체크를 해제한다
  (CLI 는 `--no-igv`). 그러면 취득값만 보여 주고, **'누적에 반영' 을 체크해도
  저장하지 않는다.** 기본은 실시(체크)다.
- 이미 잘못 넣었으면 `python -m wirye_capacity delete --date <날짜>` 로 지운다.

> `W=0` 을 미실시 표식으로 쓸 수 없다. 극저온 구간(CIT<0)은 IGV 를 실시해도 온도밴드
> 기본값이 0 이다. 그래서 툴이 별도 플래그를 받는다.

**③ 전체 미리보기 (원본 안 바뀜)**

```powershell
python scripts\commission.py --walk
```

사본 DB 로 6회차를 순서대로 다 돌려 종합 판정까지 보여 준다. 여기서 이상한 건이
보이면(계산 일치 ❌, 미달, RH 이상치) 실제 반영 전에 담당자와 먼저 확인한다.

---

## 3. 회차 절차 (6회 반복)

### STEP 1 — 취득 일치 확인 (RiMS 접속 가능할 때만)

툴이 그 날짜를 직접 취득해 담당자 표와 같은지 본다. 이걸 먼저 하는 이유는, CC실측이
1 MW 틀리면 보정값이 그대로 1 MW 틀리기 때문이다.

```powershell
python -m wirye_capacity check-rims --date 2026-05-12 --start 15:00
```

취득 시각이 표의 **`Data 취득 시작~종료 시간`** 과 같아야 한다(대부분 15:00 또는 16:00
시작 1시간). 다르면 값이 달라진다.

여러 건을 한 번에 보려면:

```powershell
python scripts\source_compare.py --date 2026-05-12 --start 15:00
```

**판정**: CIT·대기압·RH·CC 가 소수 4자리까지 일치 → 통과. 어긋나면 `--start` 를 표와
맞춰 다시 확인하고, 그래도 다르면 담당자와 수식(fnTagStat vs eDNA)을 확인한다.
2025-10-28 이 그런 사례였다.

### STEP 2 — 예측·대조 (DB 안 바꿈)

```powershell
python scripts\commission.py --step
```

가장 이른 미반영 건 1개를 잡아 ①② 검증과 세 방법의 예측을 한 화면에 보여 준다.

```
② 예측 정확도 (반영 전 예측 vs 실측)
   실측 신고값(Net) 418.90 MW    허용밴드 ±0.5%
   방법             예측 보정    예측 Net     실측-예측  판정
   구간평균          +2.619    418.39     +0.51  ✅ 밴드 내
   커널회귀          +3.529    419.30     -0.40  ✅ 밴드 내
   GP            +3.105    418.88     +0.02  ✅ 밴드 내
```

### STEP 3 — 판정

| 표시 | 뜻 | 조치 |
|---|---|---|
| ✅ 밴드 내 | \|실측−예측\| ≤ 0.5% | 통과 |
| 밴드 위(안전) | 실측이 예측보다 높음 | 통과. 기회손실이지만 페널티 없음 |
| ⚠ 미달 | 실측 < 예측−0.5% | **문제.** 신고량을 못 지킨 경우다 |
| ❌ 계산 일치 | 표와 0.25 MW 초과 차이 | **반영하지 말고** 원인부터 |

`이 구간 실측 N건뿐` 같은 안내가 나오면 그 구간 근거가 얇다는 뜻이니 함께 적어 둔다.

### STEP 4 — 반영

판정이 납득되면 반영한다. 이때 그 회차의 예측값이 기록에 남는다.

```powershell
python scripts\commission.py --step --apply
```

### STEP 5 — 다음 회차

STEP 1 로 돌아간다. 6회를 마치면 남은 회차 0으로 표시된다.

---

## 4. 종합 판정

```powershell
python scripts\commission.py --walk      # 백업 DB 로 돌리면 전 회차 종합 판정
python scripts\commission.py --report    # 회차 기록 원본(CSV)
```

```
방법              평균오차      최대오차     밴드 내     미달
구간평균           1.462     2.644      3/6     1건
커널회귀           0.904     1.624      6/6     0건
GP             0.938     2.518      4/6     0건
```

**판정 기준은 미달 0건이 최우선**이고, 그 다음이 평균오차다. 미달은 페널티로 이어지고
밴드 위는 기회손실에 그치므로 두 오차의 무게가 다르다.

이걸로 결정할 것 두 가지.

1. **보정 방법 확정** — 6건에서 가장 나은 쪽으로 고정한다. 화면에서 고르면 저장되어
   다음 실행에도 유지된다. 구간평균은 담당자 실적표와 계산이 같아 소명이 쉬운 장점이
   따로 있으니, 오차 차이가 0.1 MW 수준이면 그 점도 함께 본다.
2. **안전마진 필요 여부** — 미달이 한 건이라도 있으면 `안전마진 계수 0.8` 을 켜고
   `--walk` 를 다시 돌려 미달이 0이 되는지 확인한다. 마진은 오차를 키우는 대신 전부
   안전한 방향으로 밀어낸다.

6건은 표본이 작다. 실입찰 전환 판단은 병행운전(`ONSITE_TEST.md` STEP 6)을 2~3회 더
거친 뒤에 한다.

---

## 5. 이번 회차에서 특별히 볼 것

**여름 데이터의 1년 경과.** 받은 6건은 2026-04~07 이고, 누적의 여름 데이터는 전부
2025-07~09 다. 30~41°C 구간은 지금 2025년 여름 실적만으로 채워져 있어, 이번 건들이
**1년 뒤 같은 설비가 같은 수준인지**를 처음으로 검증한다.

이미 비슷한 일이 한 번 있었다. 2025 상반기 실적은 하반기 이후보다 보정값이 4 MW 낮아
별개 시기로 판단하고 제외했다(`scripts/period_check.py`). 그래서 이번에도 같은 것을
본다.

- 6건이 **한 방향으로 치우쳐** 낮게 나오면 → 시기 차이(열화 또는 정비 이력) 신호다.
  개별 이상치가 아니므로 곡선을 그냥 끌어내리면 안 되고, 정비 이력을 확인해
  "그 시점 이후만 사용" 규칙을 검토한다.
- **흩어져서** 어긋나면 → 예측 오차 범위 문제다. 표본을 더 쌓거나 안전마진으로 대응한다.

**복수기 진공도.** 진공도는 외기 온도의 함수다(외기↑ → 냉각수↑ → 진공↑). 40건에서
`진공도 = 33.43 + 1.261×CIT, r = +0.952` 로 확인된다. 그래서 보정곡선에 진공 항을 따로
넣지 않는다 — CIT 곡선이 이미 담고 있어 이중 계산이 되고, 입찰 전날 알 수 있는 것은
예보 온도뿐이다.

그러면 **같은 온도인데 진공이 기준선보다 나쁜 회차**는 출력이 낮게 나올까? 물리적으로는
그럴 법하지만, **우리 데이터로는 확인되지 않았다.** 40건 전체에서 진공 잔차와 보정잔차의
상관은 `r = +0.050` 으로 사실상 0이고(임계 0.312), 온도에 민감할 구간만 봐도
(CIT≥20, 22건) `r = −0.234` 로 임계 0.423 에 못 미친다.

> **⚠ 이 항목은 세 번 잘못 결론 내린 이력이 있다.** n=5 에서 r=−0.85, n=4 에서
> r=−0.775, n=9 에서 r=−0.675 를 각각 근거로 "진공이 원인" 이라고 썼고 세 번 다
> 철회했다. 모두 **부분집합을 골라서 만든 상관**이었다. 가장 최근 건(2026년 · CIT≥20 ·
> n=9)은 임계값 0.666 을 0.009 차이로 겨우 넘었고, 한 건씩 빼 보면
> **9번 중 6번이 유의성을 잃는다(생존 3/9 = 33%)**. 구간을 CIT≥20 으로 자른 근거도
> 약했다 — 저온 18건 중 Net 462 상한에 걸리는 건 1건뿐이라 "저온은 상한 때문에
> 감도가 안 보인다" 는 설명이 성립하지 않는다.
>
> 그래서 `vacuum_check.py` 에 **임계 |r| 와 leave-one-out 안정성**을 항상 함께
> 출력하도록 넣었다. 안정성 80% 미달이면 자동으로 `기각` 이 찍힌다. 진공 가설을
> 다시 꺼내려면 이 표에서 통과해야 한다.

```powershell
python scripts\vacuum_check.py       # 온도 기준선 · 진공 잔차 · 임계 r · 안정성
python scripts\method_compare.py     # 날짜순 walk-forward 포함
```

누적에 아직 없는 회차는 `--add 날짜,CIT,대기압,RH,CC,진공` 으로 붙인다. 위 40건은
누적 36건 + 2026-08 4회차다.

**지금 말할 수 있는 것**
- 진공도는 거의 전부 온도로 설명된다 — CIT 와 `r = +0.952`(분산의 91%). 그래서 보정곡선에
  진공 항을 넣지 않는 판단은 그대로 유효하다(이중 계산 방지).
- 진공 잔차로 보정잔차를 예측할 수 **없다**. 미달 회차의 원인 설명에 진공도를 쓰지 않는다.
- "같은 온도대 진공이 1년 만에 +8.4 mbar 나빠졌다" 는 앞선 서술도 신뢰할 수 없다.
  그 값의 66%가 `2026-07-22` 한 건에서 나오고, 같은 온도끼리 직접 짝지으면 −0.6 과
  +9.9 로 부호가 갈린다. 연차 열화는 **미확정**이다.
- 진공도는 **Tool 계산에 전혀 들어가지 않는다**(DB 저장 · 화면 안내 문구 전용).
  위 철회는 어느 계산값도 바꾸지 않는다.

**10월 복수기 청소가 결정적인 검정이다.** `python scripts\vacuum_check.py --split 2026-10-01`
로 전/후를 비교한다. 청소로 진공이 회복되고 **같은 온도대 보정값이 함께 올라가면** 진공
영향이 처음으로 실증된다. 진공만 회복되고 보정값이 그대로면 진공 가설은 최종 기각이다.
어느 쪽이든 청소 후 회차를 쌓아야 하고, 그때까지 미달 원인은 열려 있는 것으로 둔다.

---

## 6. 되돌리기

한 건만 취소:

```powershell
python -m wirye_capacity delete --date 2026-05-12
```

처음부터 다시:

```powershell
copy wirye_measurements_31건_백업.db wirye_measurements.db
del commission_log.csv
```

---

## 7. 2026-04-15 ~ 07-29 GUI 병행운전 (2026-08 실시분)

담당자 실적표 중 **IGV Turn-up 실시분 5회차**를, DB를 2026-04-02 상태로 되돌린 뒤
한 건씩 넣어 검증한다. 입력 자료는 `tool/scripts/data/commission_2026.csv` 에 있다.

**04-21 · 05-06 · 05-12 는 검증하지 않는다.** IGV Turn-up 미실시 회차이고, 담당자
방침상 보정값에 쓰지 않는 자료다. 쓰지 않을 자료의 예측 정확도를 재는 것은 의미가
없으므로 예측 대조 대상에서도 뺀다(2026-08-25 결정). 누적 DB 에도 원래 없다.

### 준비 — DB 되돌리기

Tool → **Test 결과 List-up** 탭에서 아래 5건을 선택 → `🗑 삭제 표시` → `💾 저장`.

    2026-04-15   2026-05-27   2026-07-07   2026-07-22   2026-07-29

삭제 후 **누적 31건 / 최신 2026-04-02** 이면 준비 완료다.

### 어느 탭을 쓰는가 — `🧪 출력 시뮬레이션`

「공급가능용량 산정」 탭은 7일 예보 파일로 입찰 프로파일을 만드는 곳이라
특정 시험일 조건을 넣을 수 없다. 회차 대조는 시뮬레이션 탭에서 한다.

| 항목 | 설정 |
|---|---|
| 상대습도 RH | `기준 60% 사용` **체크 해제** 후 그날 실측 RH 입력 |
| W (IGV turn-up) | `온도밴드 자동` 유지 — 5회차 모두 CIT≥25 이므로 +6 이 나온다 |
| 복수기압 실측 | 참고용이다. 이론값에 반영되지 않는다(base 테이블에 ISO 조건으로 동결) |

`실측 CC(Gross)와 비교` 를 켜고 그날 CC Gross 를 넣으면 예측·실측 차이가 바로 나온다.

### 회차별 정답표 — Tool 출력이 이 값과 같아야 한다

누적 31건에서 출발해 회차마다 반영하며 진행한 값이다. 소수 2자리까지 일치해야 한다.

| # | 날짜 | CIT | 대기압 | RH | 누적 | 이론기준 | W | 구간평균 | 커널 | GP | 실측Net | 차(구간) | 실측보정 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 1 | 2026-04-15 | 25.7 | 1000.7 | 32.4 | 31 | 406.20 | +6 | 399.51 | 400.56 | 399.92 | 401.50 | +1.99 | −0.700 |
| 2 | 2026-05-27 | 25.4 | 993.4 | 74.1 | 32 | 402.08 | +6 | 395.89 | 396.66 | 396.30 | 397.50 | +1.61 | −0.579 |
| 3 | 2026-07-07 | 32.4 | 994.9 | 68.5 | 33 | 378.82 | +6 | 374.50 | 374.26 | 373.97 | 374.20 | −0.30 | −0.620 |
| 4 | 2026-07-22 | 28.7 | 997.5 | 58.1 | 34 | 394.15 | +6 | 388.29 | 388.89 | 387.73 | 385.30 | −2.99 | −4.855 |
| 5 | 2026-07-29 | 31.7 | 999.8 | 62.8 | 35 | 383.97 | +6 | 379.62 | 379.00 | 378.48 | 379.10 | −0.52 | −0.873 |

반영을 마치면 **누적 36건** — 원래 상태로 돌아온다.

### 5회차 종합 (미리 계산해 둔 값)

| 방법 | 평균오차 | 최대오차 | 밴드 내 | 미달 |
|---|---|---|---|---|
| 구간평균 | 1.481 | 2.986 | 4/5 | 1건 |
| 커널회귀 | **1.104** | 3.590 | 4/5 | 1건 |
| GP | 1.210 | **2.428** | 4/5 | 1건 |

미달 1건은 07-22 이고 세 방법 공통이다 — 방법 선택으로는 막을 수 없고 안전마진
0.5× 를 켜야 해소된다. **5건은 방법을 확정할 표본이 아니다.** 이번 병행운전은
계산이 맞는지 확인하는 데까지만 쓰고, 방법 결정은 2~3회차 더 쌓은 뒤에 한다.

### 미리 알아둘 두 가지

- **07-22 는 세 방법 모두 미달(−2.4 ~ −3.6)이고 원인은 아직 모른다.** 진공도가
  81.9 mbar 로 온도 기준선보다 +14.2 나쁜 회차인 것은 맞지만, **그것이 원인이라는
  근거는 없다**(진공 잔차와 보정잔차 상관 r=+0.003, 위 §5 경고 참조). 세 방법이
  똑같이 틀렸다는 것은 보정 방법의 문제가 아니라는 뜻까지만 말해 준다. 원인
  미확정으로 열어 두고, 10월 청소 전/후 비교에서 다시 본다.
- **04-21 은 IGV 를 W=0 으로 놓고 계산해도 보정값이 +1.743** 로, 같은 온도대 다른
  회차(+3.7~+7.4)와 어긋난다. IGV 외에 다른 요인이 있다는 뜻이라 담당자 확인
  대기 항목으로 남긴다. 검증에서는 빼지만 원인 추적은 계속한다.

### CLI 로 한 번에 돌려보려면

    python scripts/commission.py --db <DB경로> --input scripts/data/commission_2026.csv --walk

사본 DB 로 5회차를 미리보기하며 원본은 건드리지 않는다.
